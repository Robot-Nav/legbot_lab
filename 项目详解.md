<!-- 文件用途：项目全模块技术详解文档，覆盖 RL 算法原理、通信架构、训练/部署/网关/仿真/地形工具各模块。 -->

# Legbot MuJoCo 项目文件详解

本文档按模块说明仓库中主要文件的作用，覆盖项目含义、RL 训练算法原理与公式、MuJoCo 仿真器、Isaac Lab 训练框架、C++ 部署代码、Sim2Real 串口网关与测试工具。

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 通信架构总览](#2-通信架构总览)
- [3. RL 训练算法原理与公式](#3-rl-训练算法原理与公式)
- [4. legbot/ — motrix_envs 导航训练环境](#4-legbot--motrix_envs-导航训练环境)
- [5. legbot_rl_lab/ — Isaac Lab 训练与部署](#5-legbot_rl_lab--isaac-lab-训练与部署)
- [6. serial_dds_gateway/ — Sim2Real 串口网关](#6-serial_dds_gateway--sim2real-串口网关)
- [7. simulate/ — MuJoCo 仿真器](#7-simulate--mujoco-仿真器)
- [8. unitree_robots/ — Go2 模型](#8-unitree_robots--go2-模型)
- [9. terrain_tool/ — 地形生成工具](#9-terrain_tool--地形生成工具)
- [10. example/ — SDK2 示例](#10-example--sdk2-示例)
- [11. robstride_ros_sample-main/ — RobStride ROS2 示例](#11-robstride_ros_sample-main--robstride-ros2-示例)
- [12. 日志分析与诊断](#12-日志分析与诊断)
- [13. 附录：根目录文档与运行步骤汇总](#13-附录根目录文档与运行步骤汇总)

---

## 1. 项目概述

### 1.1 项目含义

Legbot 是一台基于 RobStride 电机 + 自制串口 IMU 的四足机器人，本仓库的目标是完成从**仿真训练**到**真机部署**的完整 Sim2Real 闭环：

1. **训练阶段**：在 Isaac Lab（GPU 并行）中用 PPO 算法训练速度跟踪策略，导出 `policy.onnx` 与 `deploy.yaml`。
2. **仿真验证**：在 MuJoCo 中通过 DDS 与 C++ 控制器 `legbot_ctrl` 联合仿真，验证策略与 FSM。
3. **真机部署**：通过 `serial_dds_gateway` 把真机串口电机/IMU 桥接到 DDS，控制器无需修改即可同时服务仿真与真机。

仓库同时包含一套独立的 NumPy 导航训练环境（`legbot/`），用于研究位置/朝向跟踪任务，但奖励函数目前为占位实现。

### 1.2 项目根目录

| 文件 | 说明 |
|------|------|
| `readme.md` / `readme_zh.md` | Unitree MuJoCo 仿真器安装、Sim2Real 流程（中英文） |
| `sim2real要求.md` | PPO 四足 Sim2Real 通用清单：奖励、域随机化、观测/动作、执行器、延迟、安全 |
| `注意` | 与 `sim2real要求.md` 同源的要点清单 |
| `修改日志.md` | 6 条修改记录（06-27 DDS 改造与安全保护、06-29 EMA 滤波、06-30 奖励权重与噪声、07-01 CoM 校准、07-03 全参数梳理） |
| `键盘.md` | MuJoCo 仿真器键盘替代手柄方案（`KeyboardJoystick` 类与按键映射） |
| `项目详解.md` | 本文档 |

### 1.3 机器人模型

Legbot MJCF 定义于 [legbot/xmls/legbot.xml](file:///home/fatu08/legbot_mujoco/legbot/xmls/legbot.xml)：

- **基座** `base`：质量 6.584 kg，IMU site `trunk_imu` 位于 `(0,0,0.06)`。
- **12 关节**：4 腿 FR/FL/RR/RL × {hip, thigh, calf}，hip/thigh 力矩 ±16 N·m、calf ±32 N·m。
- **12 执行器** `motor`：顺序 FR/FL/RR/RL，calf 用 `calf_joint` class。
- **传感器**：12×jointpos、12×jointvel、12×jointactuatorfrc、framequat/gyro/accelerometer/framepos/framelinvel 绑定 `trunk_imu`。
- **默认关节角**：hip=0、thigh=0.9、calf=-1.8（站立姿态）。

---

## 2. 通信架构总览

### 2.1 整体结构

```
┌─────────────┐     DDS rt/lowcmd,rt/lowstate (1kHz)     ┌──────────────────┐
│  legbot_ctrl  │ ◄──────────────────────────────────────► │  对端 DDS 节点    │
│ (C++ 控制器) │                                          │  二选一：         │
│  FSM + RL   │                                          │  ① unitree_mujoco │
│  DDSInterface                                          │  ② serial_dds_   │
└─────────────┘                                          │     gateway      │
        │                                                 └────────┬─────────┘
        │ 50Hz UDP 19876 (187 floats, height_scan)                 │
        └─────────────────────────────► (仅 Sim2Sim 启用)           │ 串口
                                                             /dev/myttyCAN0 (FR+RR)
                                                             /dev/myttyCAN1 (FL+RL)
                                                             /dev/myttyIMU  (IMU)
```

> **重要变更**：Sim2Real 重构后，控制器侧**只剩 `DDSInterface` 一种实现**，原 `Sim2RealInterface`（控制器直接走 USB 串口）已删除（[legbot_rl_lab/deploy/robots/legbot/include/Types.h:50-72](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/include/Types.h)）。Sim2Sim 与 Sim2Real 共用同一条 DDS 通路，区别仅在对端是 MuJoCo 仿真器还是 `serial_dds_gateway`。

### 2.2 通信方式

| 方式 | 用途 | 协议 / 设备 | 频率 |
|------|------|-------------|------|
| DDS | 控制器 ↔ 仿真器/网关 | `rt/lowcmd`, `rt/lowstate`, `rt/sportmodestate`, `rt/wirelesscontroller` | 1kHz |
| UDP | 高程扫描（仅 Sim2Sim） | `127.0.0.1:19876`, 187 floats | 50Hz |
| Linux js | 物理手柄 | `/dev/input/js*` | 事件驱动 |
| GLFW | 键盘虚拟手柄 | `glfwGetKey()` | 渲染帧率 |
| USB 串口 | 电机控制/状态（网关侧） | `/dev/myttyCAN0`(FR+RR), `/dev/myttyCAN1`(FL+RL), 921600 baud | 500Hz |
| USB 串口 | IMU 数据（网关侧） | `/dev/myttyIMU`, 921600 baud | 100–200Hz |

### 2.3 Sim2Sim 与 Sim2Real 差异

| 维度 | Sim2Sim | Sim2Real |
|------|---------|----------|
| DDS 对端 | `unitree_mujoco` 仿真器 | `serial_dds_gateway` 网关进程 |
| 高程扫描 | UDP 50Hz（187 floats） | 网关返回全零 |
| IMU | MuJoCo 陀螺/加速度计 | 自制串口 IMU（网关侧解析） |
| 电机 PD | MuJoCo 执行器 | RobStride 电机固件 |
| 串口协议 | 无 | RobStride CAN 扩展帧 + IMU 32 字节帧（网关侧） |
| 手柄 | DDS WirelessController | DDS WirelessController（网关转发） |

---

## 3. RL 训练算法原理与公式

本节对应 [legbot_rl_lab/source/unitree_rl_lab/](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/)，是项目的算法核心。

### 3.1 PPO 算法原理

训练采用 PPO（Proximal Policy Optimization，近端策略优化），属于 on-policy 策略梯度算法。核心思想：每次策略更新后，新旧策略概率比被裁剪在 `[1-ε, 1+ε]` 区间内，避免一步更新过大导致训练崩溃。

**Clipped Surrogate Objective**：

```
L^CLIP(θ) = E_t [ min( r_t(θ) · Â_t,  clip(r_t(θ), 1-ε, 1+ε) · Â_t ) ]
```

其中概率比 `r_t(θ) = π_θ(a_t|s_t) / π_θ_old(a_t|s_t)`，`Â_t` 为广义优势估计（GAE）。

**GAE 优势函数**：

```
Â_t = Σ_{l=0}^{∞} (γλ)^l · δ_{t+l}
δ_t = r_t + γ·V(s_{t+1}) - V(s_t)
```

`γ` 为折扣因子，`λ` 为 GAE 平滑参数。

**值函数损失**（clipped）：

```
L^VF(θ) = E_t [ max( (V_θ(s_t) - V_target)²,  (V_θ_clip(s_t) - V_target)² ) ]
```

**总损失**：

```
L(θ) = -L^CLIP(θ) + c₁·L^VF(θ) - c₂·S[π_θ]
```

`S[π_θ]` 为策略熵，鼓励探索。

### 3.2 MDP 定义

**观测空间**（policy，45 维，[velocity_env_cfg.py:264-283](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py)）：

| 项 | 维度 | scale | 噪声 U(min,max) |
|---|---|---|---|
| base_ang_vel | 3 | 0.2 | (-0.3, 0.3) |
| projected_gravity | 3 | — | (-0.1, 0.1) |
| velocity_commands | 3 | — | — |
| joint_pos_rel（相对默认位置） | 12 | — | (-0.02, 0.02) |
| joint_vel_rel | 12 | 0.05 | (-1.5, 1.5) |
| last_action | 12 | — | — |

Critic 额外含 `base_lin_vel`(3) 与 `joint_effort`(12, scale=0.01)，共 60 维。

**投影重力公式**（部署侧 [unitree_articulation.h:27](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/include/unitree_articulation.h)）：

```
g_proj = R(q)ᵀ · (0, 0, -1)
```

`R(q)` 为基座四元数旋转矩阵，将世界重力旋转到机体系。

**动作空间**（12 维，[velocity_env_cfg.py:247-257](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py)）：

`JointPositionAction`，`scale=0.25`，`use_default_offset=True`。目标关节角：

```
q_des = q_default + 0.25 · action
```

`q_default = [0, 0.9, -1.8] × 4`（hip, thigh, calf），由执行器 PD 跟踪。

**PD 力矩公式**（[unitree.py:759-770](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/assets/robots/unitree.py)）：

```
τ = Kp · (q_des - q) + Kd · (q̇_des - q̇)
Kp = 60, Kd = 4.0
```

### 3.3 奖励函数与公式

奖励 = 任务奖励（跟踪）+ 惩罚项（平滑性、能量、姿态等），见 [velocity_env_cfg.py:303-370](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py) 与 [rewards.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/rewards.py)。

**跟踪奖励**（exp 核，σ=0.5）：

```
r_track = exp( -‖v_xy_cmd - v_xy‖² / σ² )
r_track_ang = exp( -(ω_z_cmd - ω_z)² / σ² )
```

| 奖励项 | 权重 | 公式说明 |
|--------|------|----------|
| `track_lin_vel_xy_exp` | +1.5 | 线速度跟踪，exp 核 |
| `track_ang_vel_z_exp` | +0.75 | 角速度跟踪，exp 核 |
| `feet_air_time` | +0.1 | threshold=0.5s，鼓励抬腿 |

**惩罚项**：

| 惩罚项 | 权重 | 公式 |
|--------|------|------|
| `lin_vel_z_l2` | −2.0 | `‖v_z‖²` |
| `ang_vel_xy_l2` | −0.08 | `‖ω_xy‖²` |
| `flat_orientation_l2` | −3.0 | `‖g_proj_xy‖²` |
| `joint_vel_l2` | −1e-3 | `Σ q̇_i²` |
| `joint_acc_l2` | −10e-7 | `Σ (q̇_t - q̇_{t-1})²` |
| `joint_torques_l2` | −2e-4 | `Σ τ_i²` |
| `action_rate_l2` | −0.2 | `Σ (a_t - a_{t-1})²` |
| `joint_pos_limits` | −10.0 | 超出软限位 |
| `energy` | −2e-5 | `Σ |q̇_i|·|τ_i|`（rewards.py:32-41） |
| `joint_position_penalty` | −0.7 | `‖q - q_default‖²`，静止时 ×5（rewards.py:90-103） |
| `air_time_variance` | −1.0 | 四腿摆动相方差（rewards.py:208-221） |
| `feet_slide` | −0.1 | 接触期足端滑动 |
| `undesired_contacts` | −1.0 | hip/thigh/calf 接触 threshold=1 |

**总奖励**：

```
r_t = Σ_i w_i · r_i,t
```

### 3.4 域随机化

域随机化配置于 [velocity_env_cfg.py:103-220](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py)，自定义函数在 [mdp/events.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/events.py)。

| 项 | 模式 | 范围 | 行号 |
|---|---|---|---|
| 静/动摩擦 | startup | U(0.25, 1.5) | 107-117 |
| 基座附加质量 | startup | U(−1.5, 2.0) kg | 120-129 |
| COM 偏移 | startup | x±0.005, y±0.0005, z±0.0025 | 132-139 |
| Kp/Kd 缩放 | startup | U(0.9, 1.1) → Kp∈[54,66], Kd∈[3.6,4.4] | 142-152 |
| 关节摩擦系数缩放 | startup | U(0.5, 3.0)（标称 0.01） | 155-164 |
| 力矩限幅缩放 | startup | U(0.85, 1.15) | 167-174 |
| 基座外力/力矩 | reset | force ±5N, torque ±1N·m | 177-185 |
| 推动机器人 | interval 5–10s | vel x/y ±0.2 m/s | 215-220 |
| 复位基座 | reset | x/y ±0.5, yaw ±π | 188-202 |
| 复位关节 | reset | pos×1.0, vel ±1.0 | 205-212 |
| 动作延迟 | per-actuator | 0–4 步 = 0–20 ms（unitree.py:762-773） | — |

### 3.5 终止条件与命令采样

**终止**（[velocity_env_cfg.py:373-382](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py)）：

- `time_out`：达到 episode_length_s=20s
- `illegal_contact`：基座接触力 > threshold 1.0
- `bad_orientation`：基座倾斜 > 0.8 rad

**命令采样**（`UniformLevelVelocityCommandCfg`，resampling_time=10s，rel_standing_envs=0.1）：

- 初始 ranges：lin_vel_x (−0.1, 0.1)、lin_vel_y (−0.1, 0.1)、ang_vel_z (−1, 1)
- limit_ranges（课程上限）：lin_vel_x (−1.0, 1.0)、lin_vel_y (−0.6, 0.6)、ang_vel_z (−1.0, 1.0)
- 课程：`terrain_levels_vel` + `lin_vel_cmd_levels`

### 3.6 PPO 超参数与网络结构

配置见 [rsl_rl_ppo_cfg.py:11-37](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/agents/rsl_rl_ppo_cfg.py)。

| 超参数 | 值 | 说明 |
|--------|-----|------|
| num_steps_per_env | 24 | 每次 rollout 每环境步数 |
| max_iterations | 50000 | 总迭代数 |
| num_learning_epochs | 5 | 每批数据训练轮数 |
| num_mini_batches | 4 | mini-batch 数 |
| learning_rate | 1e-3 | adaptive，desired_kl=0.01 |
| clip_param (ε) | 0.2 | PPO 裁剪范围 |
| entropy_coef (c₂) | 0.01 | 鼓励探索 |
| value_loss_coef (c₁) | 1.0 | 值函数权重 |
| gamma (γ) | 0.99 | 折扣因子 |
| lam (λ) | 0.95 | GAE 平滑 |
| max_grad_norm | 1.0 | 梯度裁剪 |

**Actor / Critic 网络**：

- actor_hidden_dims = [512, 256, 128]
- critic_hidden_dims = [512, 256, 128]
- activation = "elu"
- init_noise_std = 1.0

**训练规模**（[velocity_env_cfg.py:397-422](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py)）：

- num_envs = 4096，env_spacing = 2.5
- sim.dt = 0.005s，decimation = 4 → 控制步 dt = 0.02s（50 Hz）
- episode_length_s = 20.0（1000 控制步）
- 每迭代采样 = 4096 × 24 = 98304 步
- 50000 迭代 ≈ 4.9×10⁹ 步

### 3.7 执行器模型

执行器配置见 [unitree_actuators.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/assets/robots/unitree_actuators.py)，T-N 曲线 + 摩擦模型，使仿真更接近真机电机特性。

**摩擦模型**（unitree_actuators.py:67-69）：

```
τ_actual = τ_cmd − Fs·tanh(q̇ / V_a) − Fd·q̇
```

`Fs` 为静摩擦，`Fd` 为粘滞摩擦，`V_a` 为平滑过渡速度。

**T-N 曲线（力矩-速度限幅）**：

| 执行器 | X1 | X2 | Y1=Y2 | armature |
|--------|-----|-----|-------|----------|
| LegbotHipThigh | 15 | 30 rad/s | 15 N·m | 0.0043 |
| LegbotCalf | 10 | 20 rad/s | 30 N·m | 0.04 |

- `|q̇| < X1`：τ_max = Y1（正转）/ Y2（反转）
- `X1 ≤ |q̇| ≤ X2`：τ_max 线性衰减到 0
- `|q̇| > X2`：τ_max = 0

### 3.8 策略导出

训练完成后导出两类文件：

**deploy.yaml**（训练启动时即导出，[export_deploy_cfg.py:22-117](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/utils/export_deploy_cfg.py)）：

- `joint_ids_map`：URDF 关节名 → SDK 顺序映射
- `step_dt` = sim.dt · decimation = 0.02
- `stiffness` / `damping` / `default_joint_pos`（按 SDK 顺序重排）
- `commands.base_velocity.ranges`（取 limit_ranges）
- `actions`：每项 scale、clip、offset、joint_ids
- `observations`：每项维度、scale、clip、history_length

**policy.onnx / policy.pt**（[play.py:142-152](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/scripts/rsl_rl/play.py)）：

- `export_policy_as_jit` → `policy.pt`
- `export_policy_as_onnx` → `policy.onnx`
- normalizer 取自 `actor_obs_normalizer`，嵌入 ONNX

---

## 4. legbot/ — motrix_envs 导航训练环境

`legbot/` 是基于 `motrix_envs` 框架的 NumPy 导航环境，与 `legbot_rl_lab/` 的 Isaac Lab 训练方案是两套独立系统（共用机器人模型，但任务、框架、奖励均不同）。

| 文件 | 说明 |
|------|------|
| [legbot/__init__.py](file:///home/fatu08/legbot_mujoco/legbot/__init__.py) | 环境包入口，注册 `legbot_navigation_section001`（其余环境已注释） |
| [legbot/cfg.py](file:///home/fatu08/legbot_mujoco/legbot/cfg.py) | 7 套场景配置：flat/stairs/stairs_obstacles/section01/02/03/long_course/section001 |
| [legbot/legbot_section001_np.py](file:///home/fatu08/legbot_mujoco/legbot/legbot_section001_np.py) | `VBotSection001Env` 实现 |
| `legbot/xmls/legbot.xml` | Legbot 机器人 MJCF 定义 |
| `legbot/xmls/scene*.xml` | 平地、楼梯、高台、完整赛道等场景 |
| `legbot/xmls/meshes/` | Legbot 各连杆 STL 网格（17 个） |
| `legbot/xmls/assets/` | 场景装饰模型与纹理 |

### 4.1 MDP 定义

- **观测维度 54**：base_linvel(3) + gyro(3) + projected_gravity(3) + joint_pos_rel(12) + joint_vel(12) + last_actions(12) + velocity_commands(3) + position_error(2) + heading_error(1) + distance(1) + reached_flag(1) + stop_ready_flag(1)
- **动作维度 12**：Box(-1,1)，`action_scale=0.25`，力矩由 PD 计算 `τ = kp·(target − pos) − kv·vel`，kp=80/kv=6，限幅 hip/thigh ±16、calf ±32 N·m
- **奖励**：当前 `_compute_reward` 直接返回 0（[legbot_section001_np.py:483-492](file:///home/fatu08/legbot_mujoco/legbot/legbot_section001_np.py)），cfg 中定义的 13 项奖励（position_tracking=2.0、heading_tracking=1.0、termination=−200 等）尚未实现，**当前为占位实现，无法训练**
- **终止**：base_contact > 0.01

### 4.2 step 流程

`apply_action` 保存 last_dof_vel → EMA 低通滤波(α=0.3) → `_compute_torques` 写入 `actuator_ctrls`；`update_state` 提取 root_state/joint/IMU → 计算位置与朝向误差 → P 控制生成期望 vel/yaw → 归一化拼观测 → 更新 target_marker → 算奖励(0) → 算终止。

### 4.3 场景文件

`scene.xml`(flat 默认)、`scene_stairs.xml`、`scene_stairs_obstacles.xml`、`scene_section01/02/03/001.xml`、`world.xml`/`world2.xml`(完整赛道)、`rollball.xml`/`test_rollball.xml`(滚球测试)，以及多个版本备份（`0122_C_section03.xml` 等）。

---

## 5. legbot_rl_lab/ — Isaac Lab 训练与部署

### 5.1 训练脚本

| 文件 | 说明 |
|------|------|
| [scripts/rsl_rl/train.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/scripts/rsl_rl/train.py) | Isaac Lab + RSL-RL PPO 训练入口；训练时导出 `deploy.yaml`、`env.yaml`、`agent.yaml` |
| [scripts/rsl_rl/play.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/scripts/rsl_rl/play.py) | 推理演示，导出 `policy.onnx` 与 `policy.pt` |
| [scripts/rsl_rl/cli_args.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/scripts/rsl_rl/cli_args.py) | RSL-RL 相关命令行参数 |
| [scripts/list_envs.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/scripts/list_envs.py) | 列出已注册的 RL 任务 |

### 5.2 训练源码

| 文件 | 说明 |
|------|------|
| [source/.../assets/robots/unitree.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/assets/robots/unitree.py) | 机器人资产配置，`UNITREE_LEGBOT_CFG`，Kp=60/Kd=4，执行器延迟 0–20ms |
| [source/.../assets/robots/unitree_actuators.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/assets/robots/unitree_actuators.py) | 执行器 T-N 曲线与摩擦模型，`LegbotHipThigh`、`LegbotCalf` |
| [source/.../tasks/locomotion/robots/legbot/velocity_env_cfg.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/robots/legbot/velocity_env_cfg.py) | Legbot 速度跟踪环境完整 MDP 配置（详见第 3 节） |
| [source/.../tasks/locomotion/mdp/rewards.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/rewards.py) | 自定义奖励函数实现（energy、joint_position_penalty、air_time_variance 等） |
| [source/.../tasks/locomotion/mdp/events.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/events.py) | 域随机化函数实现（力矩限幅缩放等） |
| [source/.../tasks/locomotion/agents/rsl_rl_ppo_cfg.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/agents/rsl_rl_ppo_cfg.py) | PPO 超参数与网络结构配置 |
| [source/.../utils/export_deploy_cfg.py](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/source/unitree_rl_lab/unitree_rl_lab/utils/export_deploy_cfg.py) | 从训练环境导出 `deploy.yaml` |

### 5.3 C++ 部署代码

#### 公共头文件

| 文件 | 说明 |
|------|------|
| [deploy/include/param.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/param.h) | 加载 `config.yaml`，解析命令行参数，自动查找最新策略目录 |
| [deploy/include/deploy_safety.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/deploy_safety.h) | 安全配置加载，命令侧 clip + 反馈侧切 Passive |
| [deploy/include/deploy_csv_logger.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/deploy_csv_logger.h) | 50Hz CSV 诊断日志（phase/t/grav/joint_pos/action/tau_est/temperature 等） |
| [deploy/include/LinearInterpolator.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/LinearInterpolator.h) | 线性插值器，用于 `FixStand` |
| [deploy/include/unitree_articulation.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/unitree_articulation.h) | 机器人关节/IMU 状态更新，投影重力计算 |
| [deploy/include/unitree_joystick_dsl.hpp](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/unitree_joystick_dsl.hpp) | 手柄 DSL 解析器，支持按键组合条件 |
| [deploy/include/isaaclab/algorithms/algorithms.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/isaaclab/algorithms/algorithms.h) | `OrtRunner`：ONNX Runtime 推理封装 |
| [deploy/include/isaaclab/devices/keyboard/keyboard.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/isaaclab/devices/keyboard/keyboard.h) | 终端键盘读取类 |

#### FSM 状态机

FSM 以 `dt=0.001`（1kHz）的 `RecurrentThread` 运行（[CtrlFSM.h:54](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/CtrlFSM.h)），每 tick 依次调用 `pre_run → run → post_run`，再遍历 `registered_checks` 判断是否切换状态。状态通过 `REGISTER_FSM` 宏 + 工厂 map 注册（[BaseState.h:45](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/BaseState.h)）。

| 状态 | id | 文件 | 转换条件 |
|------|----|------|---------|
| Passive | 1 | [State_Passive.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/State_Passive.h) | LT+A.on_pressed → FixStand |
| FixStand | 2 | [State_FixStand.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/State_FixStand.h) | LT+B.on_pressed → Passive；start.on_pressed → Velocity |
| Velocity (RLBase) | 3 | [State_RLBase.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/State_RLBase.h) | LT+B.on_pressed → Passive |

#### Legbot 部署程序

| 文件 | 说明 |
|------|------|
| [deploy/robots/legbot/main.cpp](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/main.cpp) | 主入口；解析 `--network`/`--csv-log`/`--log`，创建 `DDSInterface`，启动 `CtrlFSM` |
| [deploy/robots/legbot/include/Types.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/include/Types.h) | `DDSInterface`；UDP 高程扫描；手柄；安全检查；`clamp_motor_cmds` |
| [deploy/robots/legbot/src/State_RLBase.cpp](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/src/State_RLBase.cpp) | RL 状态实现，加载 `deploy.yaml` 与 `policy.onnx` |
| [deploy/robots/legbot/config/config.yaml](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/config/config.yaml) | FSM 配置 + 关节顺序 + 安全阈值 |

#### Go2 部署程序

| 文件 | 说明 |
|------|------|
| [deploy/robots/go2/main.cpp](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/go2/main.cpp) | Go2 控制器主程序 |
| [deploy/robots/go2/include/Types.h](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/go2/include/Types.h) | Go2 类型定义，仅 Sim2Sim |

### 5.4 RL 推理流程（State_RLBase）

State_RLBase 是 RL 策略的运行时（[State_RLBase.cpp](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/src/State_RLBase.cpp)）：

1. **构造**：解析 `policy_dir` → 加载 `params/deploy.yaml` 构造 `ManagerBasedRLEnv` → 加载 `exported/policy.onnx` 构造 `OrtRunner`
2. **enter()**：写入 kp=`joint_stiffness`、kd=`joint_damping`、dq=0、tau=0；启动独立 `policy_thread` 以 `step_dt`=0.02s（50Hz）循环 `env->step()`
3. **env->step()**：`robot->update()` → `observation_manager->compute()` → `alg->act(obs)` → `action_manager->process_action(action)`
4. **观测构建顺序**：`base_ang_vel`(scale 0.2, IMU 陀螺) → `projected_gravity`(scale 1.0, `R(q)ᵀ·(0,0,-1)`) → `velocity_commands`(joystick) → `joint_pos_rel` → `joint_vel_rel`(scale 0.05) → `last_action`
5. **ONNX 推理**（[algorithms.h:64](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/isaaclab/algorithms/algorithms.h)）：按 `input_names` 创建 `Ort::Value` 张量，`session->Run` 后 memcpy 到 `action`，加锁保护
6. **动作处理**（`joint_actions.h:39`）：`processed = raw * scale + offset`，再 clip。scale=0.25，offset=`[0,0,0,0,0.9,0.9,0.9,0.9,-1.8,-1.8,-1.8,-1.8]`
7. **run()**（1kHz）：取 `processed_actions` → `clamp_action` → 按 `joint_ids_map` 写入 `motor_cmds[map[i]].q`
8. **PD 力矩**：`τ = Kp·(q_des − q) + Kd·(q̇_des − q̇) + tau` **不在 deploy 计算**，而是把 kp/kd/q_des/dq_des/tau 下发，由电机固件（实机）或 MuJoCo（仿真）执行 PD

### 5.5 FixStand 插值原理

[State_FixStand.h:42](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/State_FixStand.h) `enter()` 记录当前 `motor_states[i].q` 与 `t0_`；`run()` 计算 `t = now - t0_`，调用 `linear_interpolate`（[LinearInterpolator.h:9](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/LinearInterpolator.h)）：

```
alpha = (t - ts[i]) / (ts[i+1] - ts[i])
result = ys[i]·(1 - alpha) + ys[i+1]·alpha
```

`ts=[0,1,2]`，`qs[1]=qs[2]=[0,0.9,-1.8]×4`（训练默认姿态），1 秒内线性站起。

### 5.6 安全保护机制

启动时 `load_safety_config()`（[deploy_safety.h:63](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/deploy_safety.h)）。

**命令侧**（`clamp_motor_cmds`，[Types.h:219](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/include/Types.h)）：

- action_clip = ±100
- 关节限位 `joint_pos_lower/upper`
- 力矩 ±40 N·m
- `delta_q_limit_per_tick` = 0.05 rad/tick（防 NN 突跳）
- `delta_dq_limit_per_tick` = 1.0 rad/s/tick

**反馈侧 → Passive**（[FSMState.h:52-78](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/include/FSM/FSMState.h)）：

- 通信超时
- roll/pitch > 0.5 rad
- 关节速度 > 30 rad/s
- `tau_est` > 45 N·m
- 温度 > 80°C
- 急停
- RLBase 额外：投影重力 z 轴夹角 > 1.0 rad（`bad_orientation`）

Passive 为阻尼软停（mode=1, kp=0, kd=3, q=current），非断电。

### 5.7 Sim2Real 部署架构

当前 Sim2Real 架构为三组件（详见 [deploy/sim2real_通信部署指南.md](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/sim2real_通信部署指南.md)）：

```
legbot_ctrl (1kHz RL 控制器)  ⇄  DDS rt/lowcmd/lowstate  ⇄  serial_dds_gateway (500Hz 串口网关)  ⇄  12 电机 + IMU
```

控制器侧已统一为 `DDSInterface`，串口协议细节全部由 `serial_dds_gateway` 处理（见第 6 节）。`zero_offsets`（趴姿偏置）由网关侧 `joint_prone_bias.fatu.txt` 处理，deploy 侧不感知。

---

## 6. serial_dds_gateway/ — Sim2Real 串口网关

`serial_dds_gateway/` 是 Sim2Real 路径的核心模块，把真机串口电机（RobStride RS02，经灵足 USB-CAN 适配器）和自制串口 IMU 桥接到 Unitree DDS 话题 `rt/lowcmd` / `rt/lowstate`，让原本只支持 Sim2Sim DDS 的控制器 `legbot_ctrl` 能直接用于 Sim2Real，无需改控制器代码。

### 6.1 源文件

**核心库**（编入 `libserial_dds_gateway_core.a`）：

| 文件 | 说明 |
|------|------|
| [include/protocol_codec.hpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/include/protocol_codec.hpp) / [src/protocol_codec.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/protocol_codec.cpp) | type1 编码 / type2 解码，16 位量化。`RangeSpec`：q±4π、dq±44、kp 0-500、kd 0-5、tau hip/thigh ±16 / calf ±32（`RangeSpecForMotor` 按 CAN ID 自动选择）；`build_can_id` 把 mode/data16/id8 打包成 32 位 CAN ID |
| [include/can_id_codec.hpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/include/can_id_codec.hpp) / [src/can_id_codec.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/can_id_codec.cpp) | legacy 29 位 CAN ID ↔ 4 字节扩展字段转换（右移/左移 3 位） |
| [include/serial_framer.hpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/include/serial_framer.hpp) / [src/serial_framer.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/serial_framer.cpp) | 串口 IO + 帧拆装。帧格式 `45 54 + channel + frame_type + id_field(2) + master_id + dlc + data + 0D 0A`；termios raw 模式，EIO 自动重开 |
| [include/lingzu_serial.hpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/include/lingzu_serial.hpp) | 灵足 USB-CAN 线格式常量：header `{0x45,0x54}`、tail `{0x0D,0x0A}`、standard=0x01、extended=0x02、enable=0x03、disable=0x04 |
| [include/imu_framer.hpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/include/imu_framer.hpp) / [src/imu_framer.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/imu_framer.cpp) | IMU 帧解析。线格式 `EB 90 A5 FF + yaw/pitch/roll/gz/gy/gx float32 LE + CRC16-Modbus LE + 80 7F`；`EulerYprToQuaternion` 用 ZYX 约定 |
| [include/motor_map.hpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/include/motor_map.hpp) | 12 关节 CAN ID 映射：FR=(11,21,31)/FL=(12,22,32)/RR=(13,23,33)/RL=(14,24,34)；总线 A=FR+RR，B=FL+RL |

**可执行文件**：

| 文件 | 说明 |
|------|------|
| `src/dds_to_serial_gateway.cpp` | 主网关：订阅 `rt/lowcmd`、按 mode 边沿发 type3/4、每 tick 发 type1；RX 线程解析 type2 反馈与 IMU 帧，发布 `rt/lowstate`。默认 500Hz |
| [src/legbot_rt_gait_pd.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/legbot_rt_gait_pd.cpp) | DDS-only 实时 IK+PD 步态执行器（**不接触串口**），含 stand/trot 模式、FK/IK、软限位、安全门。用于无 RL 策略时测试网关。需 `--robot-standing-supported --i-accept-risk` 才能 non-dry-run |
| [src/one_motor_demo.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/one_motor_demo.cpp) | 本地编解码自测，无串口 |
| [src/one_motor_serial.cpp](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/src/one_motor_serial.cpp) | 单电机串口 tx/rx 测试 |
| `src/twelve_motor_serial.cpp` | 双口 12 电机使能 + type1 收发测试，支持 `--commands-file` CSV |
| `src/lingzu_frame_verify.cpp` / `src/imu_frame_verify.cpp` | 黄金帧回归测试 |
| `src/imu_serial_monitor.cpp` | 实时 IMU 串口监听（`--degrees`、`--duration`） |

**其他**：

| 文件 | 说明 |
|------|------|
| [web/serial_frame_codec.js](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/web/serial_frame_codec.js) | JavaScript 版协议编解码，与 C++ 完全对齐，配合 `serial_frame_tester.html` 在浏览器调试帧 |
| [can_id_codec.py](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/can_id_codec.py) | Python 版 CAN ID 编解码 |
| [CMakeLists.txt](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/CMakeLists.txt) | 构建脚本 |
| [README.md](file:///home/fatu08/legbot_mujoco/serial_dds_gateway/README.md) | 使用说明 |
| `build.sh` / `start_gateway.sh` / `start_ctrl.sh` / `check_buffer.sh` | 构建/启动/检查脚本 |

### 6.2 关键协议

**RobStride CAN 扩展帧**（type1 命令 / type2 反馈 / type3 使能 / type4 失能）：

-| type1：标准帧，torque 放 id_field
| type2：扩展帧，反馈位置/速度/力矩/温度
| type3/type4：mode 边沿触发使能/失能，type4 data[0]=1 清故障
- 16 位量化：q±4π、dq±44、kp 0-500、kd 0-5、tau hip/thigh ±16 / calf ±32（`RangeSpecForMotor` 按 CAN ID 自动选择）

**IMU 32 字节帧**：`EB 90 A5 FF + 6×float32(yaw,pitch/roll,gz,gy,gx) + CRC16-Modbus(LE) + 80 7F`

**网关 4 道工序**：

1. mode 边沿使能（type3/type4）
2. 16 位量化（type1）
3. joint bias 模型↔电机空间转换：左侧 FL/RL thigh+calf 符号翻转，小腿 2:1 减速
4. IMU 欧拉角→四元数 + 陀螺滤波（scheme A 静止偏置标定 + 死区滤波）

### 6.3 编译与运行

```bash
cd ~/legbot_mujoco/serial_dds_gateway
./build.sh                      # 清空 build/ 并 cmake + make

# 终端 1：启动网关（依赖 udev 软链接 /dev/myttyCAN0/1/IMU）
./start_gateway.sh
# 或显式参数：
./build/dds_to_serial_gateway --network lo \
  --serial-port-a /dev/myttyCAN0 --serial-port-b /dev/myttyCAN1 \
  --tick-hz 500 --send-disable-on-exit

# 终端 2：启动控制器
cd ~/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/build
./legbot_ctrl --network lo
./legbot_ctrl --network lo --csv-log   # 50Hz CSV 诊断
```

`check_buffer.sh` 检查 `/dev/myttyCAN0/1/IMU` 的 stty/udev 信息及内核 pty/ldiscs。

---

## 7. simulate/ — MuJoCo 仿真器

基于 MuJoCo + Unitree SDK2 DDS 的 C++ 仿真器，用于在连接真实机器人之前验证控制器。

### 7.1 源码文件

| 文件 | 说明 |
|------|------|
| [simulate/src/main.cc](file:///home/fatu08/legbot_mujoco/simulate/src/main.cc) | 仿真器主程序；启动物理线程与 DDS Bridge 线程；处理键盘事件 |
| [simulate/src/legbot_bridge.h](file:///home/fatu08/legbot_mujoco/simulate/src/legbot_bridge.h) | DDS 与 MuJoCo 数据桥接：订阅 `LowCmd`、发布 `LowState` / `SportModeState` / `WirelessController`；1kHz `RecurrentThread` |
| [simulate/src/param.h](file:///home/fatu08/legbot_mujoco/simulate/src/param.h) | YAML 配置与命令行参数解析 |
| [simulate/src/physics_joystick.h](file:///home/fatu08/legbot_mujoco/simulate/src/physics_joystick.h) | Xbox / Switch / Keyboard 手柄抽象 |
| [simulate/src/joystick/joystick.cc](file:///home/fatu08/legbot_mujoco/simulate/src/joystick/joystick.cc) | Linux `/dev/input/js*` 读取与解析 |
| [simulate/src/joystick/jstest.cc](file:///home/fatu08/legbot_mujoco/simulate/src/joystick/jstest.cc) | 手柄测试工具 |
| [simulate/config.yaml](file:///home/fatu08/legbot_mujoco/simulate/config.yaml) | 仿真器配置：场景文件、DDS domain_id、网卡、手柄类型 |
| [simulate/CMakeLists.txt](file:///home/fatu08/legbot_mujoco/simulate/CMakeLists.txt) | 仿真器构建脚本 |

### 7.2 键盘映射

当 `joystick_type` 设为 `keyboard` 时（详见 [键盘.md](file:///home/fatu08/legbot_mujoco/键盘.md)）：

| 按键 | 对应手柄功能 |
|------|--------------|
| W / S | 左摇杆 Y 轴 |
| A / D | 左摇杆 X 轴 |
| ↑ / ↓ | 右摇杆 Y 轴 |
| ← / → | 右摇杆 X 轴 |
| Q / E | LT / RT |
| Space | A |
| Left Shift | B |
| F / G | X / Y |
| C / V | LB / RB |
| Enter | Start |
| Tab | Back |
| 1 / 2 | F1 / F2 |

实现方式：新增 `KeyboardJoystick` 类用 `glfwGetKey` 轮询，在 `simulate.cc::UiEvent` 开头拦截按键避免冲突。

### 7.3 Sim2Sim 运行

```bash
# 编译仿真器
cd ~/legbot_mujoco/simulate/build
cmake ..
make -j$(nproc)

# 编译控制器
cd ~/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot
mkdir -p build && cd build
cmake -S .. -B .
make -j$(nproc)

# 终端 1：启动仿真器
cd ~/legbot_mujoco/simulate/build
./unitree_mujoco

# 终端 2：启动控制器
./legbot_ctrl --mode sim2sim --network lo
```

默认 `config.yaml` 加载 Legbot 场景（`legbot/xmls/scene.xml`）。

---

## 8. unitree_robots/ — Go2 模型

| 文件 | 说明 |
|------|------|
| [unitree_robots/go2/go2.xml](file:///home/fatu08/legbot_mujoco/unitree_robots/go2/go2.xml) | Go2 MJCF 模型 |
| [unitree_robots/go2/scene.xml](file:///home/fatu08/legbot_mujoco/unitree_robots/go2/scene.xml) | Go2 平地场景 |
| [unitree_robots/go2/scene_terrain.xml](file:///home/fatu08/legbot_mujoco/unitree_robots/go2/scene_terrain.xml) | Go2 地形场景 |
| `unitree_robots/go2/assets/` | Go2 OBJ 网格 |

---

## 9. terrain_tool/ — 地形生成工具

| 文件 | 说明 |
|------|------|
| [terrain_tool/terrain_generator.py](file:///home/fatu08/legbot_mujoco/terrain_tool/terrain_generator.py) | 参数化生成楼梯、杂乱地面、高程图等地形 |
| [terrain_tool/scene.xml](file:///home/fatu08/legbot_mujoco/terrain_tool/scene.xml) | 地形工具模板场景 |
| [terrain_tool/readme_zh.md](file:///home/fatu08/legbot_mujoco/terrain_tool/readme_zh.md) | 使用说明 |

---

## 10. example/ — SDK2 示例

演示如何用 Unitree SDK2 控制 Go2 完成"站起再趴下"。

| 文件 | 说明 |
|------|------|
| `example/cpp/stand_go2.cpp` | C++ SDK2 示例 |
| `example/cpp/CMakeLists.txt` | C++ 示例构建脚本 |
| `example/python/stand_go2.py` | Python SDK2 示例 |
| `example/ros2/src/stand_go2.cpp` | ROS2 示例 |
| `example/ros2/include/motor_crc.h` | 电机 CRC 校验头 |
| `example/ros2/src/motor_crc.cpp` | 电机 CRC 校验实现 |

---

## 11. robstride_ros_sample-main/ — RobStride ROS2 示例

| 文件 | 说明 |
|------|------|
| `robstride_ros_sample-main/src/main.cpp` | RobStride 电机 ROS2 控制示例 |
| `robstride_ros_sample-main/src/motor_cfg.cpp` | 电机配置实现 |
| `robstride_ros_sample-main/include/motor_ros2/motor_cfg.h` | 配置头文件 |
| `robstride_ros_sample-main/CMakeLists.txt` | 构建脚本 |
| `robstride_ros_sample-main/package.xml` | ROS2 包描述 |
| `robstride_ros_sample-main/README.md` | 使用说明 |

---

## 12. 日志分析与诊断

### 12.1 日志文件

`log/` 目录存放 `legbot_ctrl --csv-log` 输出的 50Hz 诊断 CSV，字段含：`t`、`vx_cmd`、`grav_x/y`、`q_target`/`q_actual`/`tau_est`/`kp`/`kd`（各 12 通道）、`experiment_group`、`temperature` 等。配套 PNG 为分析脚本输出。

- `log/analysis_0701/`：torque_roll、velocity_overview
- `log/analysis_0704/`：fl_weakness、front_rear_posture、pitch_roll_timeseries
- `log/fixstand_*.csv` / `log/run_*.csv`：FixStand 与 RL 运行记录

### 12.2 分析脚本

[log/analyze_0704.py](file:///home/fatu08/legbot_mujoco/log/analyze_0704.py) 诊断 7.4 实测三个问题：整机前俯（疑 URDF base COM 偏前）、前进时 FL 弱腿、后退正常。

脚本流程：

1. 读取 4 个 CSV（fix1/fix2/run1/run2），按 `vx_cmd` 分段（forward>0.15 / backward<-0.15 / zero）
2. `pitch = arcsin(grav_x)` 统计
3. FL vs FR 四腿 `|tau|`、跟踪误差、`q_std` 对比
4. 前后腿姿态差 `thigh_diff` / `calf_diff`
5. 输出 4 张图：pitch_roll 时序、前进/后退力矩柱状、FL 弱腿高亮、前后姿态差时序
6. 结论摘要

> 注：policy 关节序为 hip/thigh/calf 各 [FL, FR, RL, RR]。

---

## 13. 附录：根目录文档与运行步骤汇总

### 13.1 根目录文档索引

| 文件 | 内容 |
|------|------|
| [sim2real要求.md](file:///home/fatu08/legbot_mujoco/sim2real要求.md) | PPO 四足 Sim2Real 通用清单：奖励、域随机化、观测/动作、执行器、延迟、安全保护、测试流程。强调观测禁用特权信息（base_lin_vel、friction、terrain height），动作 `q_des=default+scale*action`，部署侧必加关节限位/力矩限幅/急停 |
| [修改日志.md](file:///home/fatu08/legbot_mujoco/修改日志.md) | 6 条修改记录：06-27 DDS 改造+11 项安全保护；06-29 EMA 滤波 α=0.3 + FixStand kd 4→3；06-30 action_rate×5、joint_acc×40、joint_vel_rel 噪声 ±2→±1；07-01 CoM 校准 `(0.026,0,0.025)→(0,0,0.04)→(-0.02,0,-0.008)` + 惯性张量平行轴修正 + damping 3→4 + EMA 换 One-Euro Filter；07-03 全参数梳理 |
| [键盘.md](file:///home/fatu08/legbot_mujoco/键盘.md) | MuJoCo 键盘替代手柄方案（见 7.2） |
| [注意](file:///home/fatu08/legbot_mujoco/注意) | 与 `sim2real要求.md` 同源的要点清单 |
| [legbot_rl_lab/项目分析文档.md](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/项目分析文档.md) | Isaac Lab 训练 + C++ 部署全链路分析 |
| [legbot_rl_lab/deploy/sim2real_通信部署指南.md](file:///home/fatu08/legbot_mujoco/legbot_rl_lab/deploy/sim2real_通信部署指南.md) | 三组件 Sim2Real 架构、串口分组、SSH 连香橙派、udev 固定名、11 项安全保护 |

### 13.2 完整运行步骤

#### A. 训练（Isaac Lab + PPO）

```bash
cd ~/legbot_mujoco/legbot_rl_lab
# 训练（导出 deploy.yaml）
python scripts/rsl_rl/train.py --task=unitree-legbot-velocity --num_envs=4096 --headless

# 推理 + 导出 ONNX
python scripts/rsl_rl/play.py --task=unitree-legbot-velocity --num_envs=1
# 输出：<log_dir>/exported/policy.onnx, policy.pt
```

#### B. Sim2Sim（MuJoCo + 控制器）

```bash
# 1. 编译仿真器
cd ~/legbot_mujoco/simulate/build && cmake .. && make -j$(nproc)

# 2. 编译控制器
cd ~/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot
mkdir -p build && cd build && cmake -S .. -B . && make -j$(nproc)

# 3. 终端 1：启动仿真器
cd ~/legbot_mujoco/simulate/build && ./unitree_mujoco

# 4. 终端 2：启动控制器
./legbot_ctrl --mode sim2sim --network lo
```

#### C. Sim2Real（网关 + 控制器）

```bash
# 1. 编译网关
cd ~/legbot_mujoco/serial_dds_gateway && ./build.sh

# 2. 编译控制器（同上）

# 3. 终端 1：启动网关（依赖 udev 软链接 /dev/myttyCAN0/1/IMU）
./start_gateway.sh

# 4. 终端 2：启动控制器
cd ~/legbot_mujoco/legbot_rl_lab/deploy/robots/legbot/build
./legbot_ctrl --network lo
./legbot_ctrl --network lo --csv-log   # 50Hz CSV 诊断
```

手柄操作：LT+A 站起 → start 交策略 → LT+B 软停。

#### D. 仿真器手柄测试

```bash
cd ~/legbot_mujoco/simulate/build && ./jstest
```

### 13.3 Legbot 与 Go2 硬件差异

| 维度 | Go2 | Legbot |
|------|-----|------|
| 通信 | Unitree DDS（内置） | DDS（经 `serial_dds_gateway` 桥接串口） |
| 电机 | 宇树自研 | RobStride RS02 |
| IMU | DDS 内置 | 自制串口 IMU（32 字节帧） |
| 输入 | DDS WirelessController | DDS WirelessController（网关转发） |
| 电机顺序 | Go2 SDK 标准 | FR → FL → RR → RL |
| 电机 PD | 宇树固件 | RobStride 固件 |

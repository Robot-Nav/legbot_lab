# 模型空间关节偏置（joint-model），电机顺序 FR/FL/RR/RL hip/thigh/calf。
# bias[i] = gear_scale[i] * q_motor_实测[i] - q_参考[i]
# 标定完成后，将一行 12 个浮点数复制到 joint_prone_bias.txt。
#
# 可从网关日志获取：
#   [INFO] joint bias (joint-model space): [ ... ]
#
# 或手动计算：bias = "减速后关节 q" - 参考姿态（见 joint_prone_reference.example.txt）。
#
# 以下为占位符（全 0 表示无额外偏置），请替换为机器人实际标定值。

0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000, 0.0000
